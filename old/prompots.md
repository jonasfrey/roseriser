we have many 'apps' that do similar or almost same things. so i want to have one app that does it all. 
the main functionality is to be able to select a dxf file. 
then this dxf information gets converted to javscript objects. 
then these javascript objects are analysed / new objects are added/calcuated , for example are and if so how are dxf entities connected. 
finally a new source code can be generated . a code that depends on these dxf entities which are hardcoded in the generated source code. 
i now need a app where i can select a dxf. 
after selecting it the javascript objects are calculated. 
finally there are many different .scad codes that are generated and can be looked at via switching tabs. there are also tabs for all svgs that can be generated. 

